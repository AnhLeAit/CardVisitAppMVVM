<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled -->
    <item android:state_enabled="false"
        android:color="@color/colorSearchHint" />

    <!-- Default color -->
    <item android:state_enabled="true"
        android:color="@color/colorTextAccent" />

</selector>